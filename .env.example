# Copy to .env (git-ignored) and fill in. Loaded automatically by the runtime; never committed.
OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://api.openai.com/v1
# ANTHROPIC_API_KEY=sk-ant-...                 # needed only for anthropic/* models
# LIGHTHOUSE_AUDITOR_MODEL=anthropic/claude-opus-5   # put the fairness auditor on a different vendor from the analyst
# LIGHTHOUSE_ANALYST_MODEL=openai/gpt-5-5
# TRUEFORGE_BASE_URL=http://localhost:8790   # set only to run on TrueForge instead of the local runtime
# LIGHTHOUSE_DATA_DIR=data
